server {
    listen 80;
    server_name backend-gettruck.totonlionel.com;

    # Redirection HTTP → HTTPS (décommenter après certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade           $http_upgrade;
        proxy_set_header Connection        "upgrade";

        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }

    access_log /var/log/nginx/backend-gettruck.access.log;
    error_log  /var/log/nginx/backend-gettruck.error.log;
}

# Bloc HTTPS — décommenter après : sudo certbot --nginx -d backend-gettruck.totonlionel.com
# server {
#     listen 443 ssl;
#     server_name backend-gettruck.totonlionel.com;
#
#     ssl_certificate     /etc/letsencrypt/live/backend-gettruck.totonlionel.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/backend-gettruck.totonlionel.com/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:5000;
#         proxy_http_version 1.1;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade           $http_upgrade;
#         proxy_set_header Connection        "upgrade";
#     }
#
#     access_log /var/log/nginx/backend-gettruck.access.log;
#     error_log  /var/log/nginx/backend-gettruck.error.log;
# }